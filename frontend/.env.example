# Copy to .env.local for local dev, or set these in Vercel project env vars.

# Backend base URL. Local dev:
VITE_API_BASE=http://localhost:8000
# Production (Railway), example:
# VITE_API_BASE=https://kaiko-backend.up.railway.app

# Clerk publishable key (from Clerk dashboard)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
