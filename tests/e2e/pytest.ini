[pytest]
# Chạy Chromium hiển thị (bỏ --headed để chạy ngầm). --slowmo giúp quan sát khi demo.
addopts = --browser chromium --html=report.html --self-contained-html -v
python_files = test_*.py
python_classes = Test*
python_functions = test_*
